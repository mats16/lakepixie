resources:
  apps:
    ccbricks_app:
      name: ${var.app_name}
      description: Claude Code on Databricks
      source_code_path: ../
      #resources:
      #  - name: database-url-secret
      #    description: Database connection string
      #    secret:
      #      scope: ccbricks-${bundle.target}
      #      key: database-url
      #      permission: READ
      user_api_scopes:
        - sql
        - genie
        - catalog.catalogs:read
        - catalog.schemas:read
        - catalog.tables:read
        - catalog.connections
        - files
        - ai-gateway
        - vector-search
        - dashboards.genie
        - mcp.external
        - mcp.functions
        - mcp.genie
        - mcp.vectorsearch
        - workspace.workspace
        - apps
      permissions:
        - level: CAN_USE
          group_name: users
