command: ["npm", "run", "start", "--workspace=apps/api"]

env:
  - name: CCBRICKS_BASE_DIR
    value: /home/app
  - name: NODE_ENV
    value: production
  # When LAKEBASE_ENDPOINT is injected, use Lakebase. When not injected, use SQLite.
  - name: LAKEBASE_ENDPOINT
    valueFrom: lakebase
